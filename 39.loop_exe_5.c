#include <stdio.h>

void main() 
{
    int  start = 1,number = 1;
    printf("%d",number);
    
    while (number <= 90) 
    {
        number = number + start;
        start+=1;
        printf("%4d ", number);
    }
        // number = number + start;
        // start+=1;
        // printf("%4d ", number);

        // number = number + start;
        // start+=1;
        // printf("%4d ", number);
        
        // number = number + start;
        // start+=1;
        // printf("%4d ", number);
       
        // number = number + start;
        // start+=1;
        // printf("%4d ", number);
    
}