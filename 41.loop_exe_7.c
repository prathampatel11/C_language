#include <stdio.h>

void main() 
{
    int start=1,number = 1;
    printf("%d",number);

    while(number<=9000)
    { 
    start+=1;
    number =start*start*start;
    printf("%5d",number);
    }
    // start+=1;
    // number =start*start*start;
    // printf("%5d",number);

    // start+=1;
    // number =start*start*start;
    // printf("%5d",number);

    // start+=1;
    // number =start*start*start;
    // printf("%5d",number);


    
    
    
   
    
}