#include <stdio.h>

void main() 
{
    int start=1,number = 1;
    printf("%d",number);

    while(number<=900)
    { 
    start+=1;
    number =start*start;
    printf("%4d",number);
    }
    // start+=1;
    // number =start*start;
    // printf("%4d",number);

    // start+=1;
    // number =start*start;
    // printf("%4d",number);

    // start+=1;
    // number =start*start;
    // printf("%4d",number);


    
    
    
   
    
}